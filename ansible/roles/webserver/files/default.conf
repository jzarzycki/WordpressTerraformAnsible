server {
	listen 80;
	listen [::]:80;
    server_name www.jzarzycki.com;
    access_log /var/log/nginx/access.log;

    location / {
        return 301 https://www.jzarzycki.com;
    }
}

server {
    listen 443 ssl default_server;
	listen [::]:443 ssl default_server;
    server_name www.jzarzycki.com;
    access_log /var/log/nginx/access.log;

    ssl_certificate     /etc/letsencrypt/archive/jzarzycki.com/fullchain21.pem;
    ssl_certificate_key /etc/letsencrypt/archive/jzarzycki.com/privkey21.pem;
    ssl_protocols       TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    location / {
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_pass http://wordpress;
    }
}
